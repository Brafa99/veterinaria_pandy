import 'package:flutter/material.dart';

class CajaPagePlaceholder extends StatelessWidget {
  const CajaPagePlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Caja (en desarrollo)",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}